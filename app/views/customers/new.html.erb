<% @page_title = 'Create Customer' %>


<div>
  <h2>Create Customer</h2>

  <%= form_for(@customer) do |f| %>

      <%= render(:partial => 'form', :locals => {:f => f} ) %>

      <div class="form-buttons">
        <%= submit_tag('Create Customer') %>
      </div>

  <% end %>
  <%= link_to("Sign in","/access/login") %>
</div>