<% @page_title = "Show Customer" %>


<div>
  <h2>Show Profile</h2>

  <table>
    <tr>
      <th>First Name</th>
      <td><%= @customer.firstname %></td>
    </tr>
    <tr>
      <th>Last Name</th>
      <td><%= @customer.lastname %></td>
    </tr>
    <tr>
      <th>E-Mail</th>
      <td><%= @customer.email %></td>
    </tr>
    <tr>
      <th>UserName</th>
      <td><%= @customer.firstname %> <%= @customer.lastname %></td>
    </tr>
  </table>
</div>
<%= link_to("Edit Profile",edit_customer_path(session[:user_id])) %>

